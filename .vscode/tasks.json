{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build ts",
            "type": "shell",
            "command": "bin\\esbuild --bundle music.ts --outdir=static --sourcemap --watch"
        }
    ]
}